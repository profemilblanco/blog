---
title: Objetivo 1
sidebar: mydoc_sidebar
permalink: 359__obj_01.html
folder: 359
---

## Información del Plan de Curso

### Objetivo
"Formular problemas de Programación Entera".

### Punturación
1 punto

## Lista de Contenido

### Planteamiento del problema de Programación Entera

La programación entera es una técnica matemática utilizada para resolver problemas de optimización en los que algunas o todas las variables de decisión deben tomar valores enteros. Este tipo de problemas surge en contextos donde la naturaleza discreta de las decisiones es fundamental, como en la asignación de recursos, la planificación de producción o la toma de decisiones binarias (sí/no). El planteamiento inicial implica identificar las variables de decisión, el objetivo a maximizar o minimizar y las restricciones que limitan las posibles soluciones.

### Formulación matemática del problema de Programación Entera

La formulación matemática de un problema de programación entera se basa en la estructura general de la programación lineal, con la adición de restricciones que imponen que algunas o todas las variables sean enteras. Matemáticamente, el problema puede expresarse como:

**Maximizar/Minimizar:**

$$
z = f_0(x_1, x_2, \dots, x_n)
$$

**Sujeto a las restricciones:**

$$
f_i(x_1, x_2, \dots, x_n) \leq b_i, \quad i \in M = \{1, 2, \dots, m\}
$$

$$
x_j \geq 0, \quad j \in N = \{1, 2, \dots, n\}
$$

Además, se incluye la condición de enteros:

- Si $$ I = N $$, todas las variables son enteras ($$ X_j \in \mathbb{Z} $$), y el problema es **puro**.
- Si $$ I \neq N $$, solo algunas variables son enteras ($$ X_j \in \mathbb{Z}, \forall j \in I $$), y el problema es **mixto**.

En el caso de programación entera lineal (PEL), la función objetivo y las restricciones son lineales:

$$
\text{Maximizar/Minimizar: } z = \sum_{j \in N} c_j x_j
$$

$$
\text{sujeto a: } \sum_{j \in N} a_{ij} x_j + h_i = b_i, \quad i \in M
$$

$$
h_i \geq 0, \quad i \in M
$$

$$
x_j \geq 0, \quad j \in N
$$

$$
x_j \text{ entero, } j \in I \subset N
$$

Donde $$ h_i $$ son las variables de holgura. La introducción de la restricción entera complica notablemente el espacio de soluciones, ya que elimina la convexidad del conjunto factible, lo cual dificulta el uso de métodos como el simplex estándar.

### Resolución del problema por redondeo.

La resolución de problemas de programación entera mediante el método de redondeo consiste en resolver primero un modelo continuo (programación lineal relajada) y luego "redondear" la solución óptima obtenida para obtener una solución entera. Este enfoque es útil debido al éxito de los métodos de resolución de programas continuos y la ineficacia computacional de algunos algoritmos para problemas enteros. Sin embargo, no todos los problemas de programación entera pueden resolverse de esta manera debido a las limitaciones inherentes al método de redondeo.

**Clasificación de los modelos enteros según la aplicabilidad del redondeo:**

#### Modelos enteros discretos:

En estos modelos, las variables de decisión son "discretamente" cuantificables pero no aceptan valores fraccionarios. Ejemplos incluyen problemas prácticos donde actividades o recursos como máquinas, transportes o operadores son indivisibles. Existe una clase especial de modelos enteros directos (como el problema de transporte), que satisfacen la propiedad unimodular, garantizando que toda solución básica sea entera. Estos modelos son los más susceptibles a la aplicación eficiente del método de redondeo.

- **Ejemplo:** Si la solución continua indica que se necesitan 10.1 autobuses para cubrir una ruta, se puede redondear a 10 autobuses. El error depende del valor continuo original.

#### Modelos enteros codificados:
En estos modelos, las variables de decisión describen aspectos cualitativos o relaciones con un número finito de posibilidades, generalmente representadas por variables binarias (0-1). Las variables binarias expresan decisiones tipo "sí/no", donde 1 significa "sí" y 0 significa "no". Dado que estas variables no tienen sentido fraccionario, el redondeo no es apropiado y puede llevar a soluciones inválidas.

- **Ejemplo:** Si una variable binaria $$ x $$ representa la aceptación de un proyecto ($$ x = 1 $$ si se acepta, $$ x = 0 $$ si se rechaza), cualquier valor fraccionario de $$ x $$ carece de significado lógico.

#### Modelos enteros transformados:

Estos modelos, en principio, podrían no incluir variables enteras, pero su resolución requiere la introducción de variables enteras artificiales (por ejemplo, variables binarias) para manejar restricciones lógicas que no pueden ser tratadas directamente por métodos existentes. Por ejemplo, cuando una de dos restricciones debe ser satisfecha, una variable binaria puede activar o desactivar una restricción sin modificar la relación lógica inicial. En este caso, el redondeo no es adecuado porque las variables binarias deben mantener sus valores exactos (0 o 1).

- **Ejemplo:** Si una variable binaria $$ x $$ controla la activación de una restricción, cualquier valor fraccionario de $$ x $$ sería inconsistente con la lógica del problema.

#### **Limitaciones del método de redondeo:**

No todas las soluciones redondeadas son factibles para el problema entero original.

- La solución redondeada no garantiza ser la solución óptima del problema entero.
- El redondeo puede violar restricciones originales, especialmente en modelos con ecuaciones.
- Solo las variables básicas pueden ser redondeadas, mientras que las variables no básicas permanecen en cero, lo que puede generar soluciones diferentes a la original.

### Ejemplos de Aplicaciones de los Modelos de Programación Entera

Este apartado presenta dos ejemplos clave de aplicaciones prácticas de la programación entera: problemas de presupuesto y problemas de secuencia.

#### Problemas de Presupuesto

Los problemas de presupuesto involucran la selección óptima de un conjunto de proyectos de inversión, sujeto a restricciones de presupuesto. El objetivo es maximizar el valor total de los proyectos seleccionados, considerando las limitaciones financieras disponibles en cada período de tiempo.

**Modelo Matemático:**
- **Variables de decisión:** $$ x_j $$ (variables binarias):

  $$
  x_j = 
  \begin{cases} 
  1, & \text{si el proyecto } j \text{ es seleccionado}, \\
  0, & \text{en cualquier otra situación}.
  \end{cases}
  $$
  
- **Función objetivo:** Maximizar el valor esperado de los proyectos seleccionados:

  $$
  \text{Maximizar } z = \sum_{j=1}^n \mathbb{E}(C_j) x_j
  $$
  donde $$ C_j $$ es el valor actual del proyecto $$ j $$.
  
- **Restricciones de presupuesto:** Garantizar que la inversión total no exceda el capital disponible en cada período $$ t $$:

  $$
  \sum_{j=1}^n \mathbb{E}(a_{tj}) x_j \leq \mathbb{E}(b_t), \quad t = 1, 2, \dots, m
  $$
  donde $$ a_{tj} $$ es la inversión requerida por el proyecto $$ j $$ en el período $$ t $$, y $$ b_t $$ es el capital disponible en el período $$ t $$.

- **Otras restricciones:** Se pueden incluir restricciones adicionales relacionadas con recursos adicionales o otras limitaciones específicas del problema.

**Consideraciones:**

Los problemas de presupuesto generalmente no son determinísticos, ya que los parámetros ($$ C_j $$, $$ a_{tj} $$, $$ b_t $$) suelen ser variables aleatorias. Para obtener un modelo determinístico, se reemplazan estas variables aleatorias por sus valores esperados.

#### Problemas de Secuencia

Los problemas de secuencia se refieren a la optimización del orden en que deben realizarse un conjunto de operaciones en una máquina o taller, con el objetivo de minimizar el tiempo total de procesamiento o cumplir con fechas de entrega establecidas. Este tipo de problemas introduce restricciones de orden, no interferencia y fecha de entrega.

**Modelo Matemático:**
- **Variables de decisión continuas:** $$ x_j $$ (tiempo de inicio de la operación $$ j $$).
- **Variables de decisión binarias:** $$ y_{ij} $$ (indicador de precedencia entre operaciones $$ i $$ y $$ j $$):

  $$
  y_{ij} = 
  \begin{cases} 
  1, & \text{si la operación } i \text{ precede a la operación } j, \\
  0, & \text{en cualquier otro caso}.
  \end{cases}
  $$

**Restricciones:**
1. **Restricciones de orden:** Garantizan que si la operación $$ i $$ precede a la operación $$ j $$, entonces:

   $$
   x_i + a_i \leq x_j
   $$
   donde $ a_i $ es el tiempo de procesamiento de la operación $$ i $$.

3. **Restricciones de no interferencia:** Evitan que dos operaciones ocupen la máquina simultáneamente. Estas restricciones se modelan mediante la variable binaria $$ y_{ij} $$:

   $$
   M y_{ij} + (x_i - x_j) \geq a_i
   $$

   $$
   M (1 - y_{ij}) + (x_j - x_i) \geq a_i
   $$
   donde $$ M $$ es una constante suficientemente grande.

5. **Restricciones de fecha de entrega:** Aseguran que cada operación termine antes de una fecha límite $$ d_j $$:

   $$
   x_j + a_j \leq d_j
   $$

- **Función objetivo:** Minimizar el tiempo total requerido para completar todas las operaciones:

  $$
  \text{Minimizar } z = t
  $$
  donde $$ t $$ es el tiempo total requerido.

**Características del Modelo:**

- El modelo resultante es mixto, ya que contiene tanto variables continuas ($$ x_j $$) como variables enteras ($$ y_{ij} $$).
- La introducción de restricciones de la forma "o bien" hace que el problema abandone la convexidad, lo cual dificulta su resolución utilizando métodos estándar de programación lineal.

#### Problema del morral o Problema del cargamento (Knapsack problem)
El problema del morral, también conocido como problema del cargamento, es un problema clásico de optimización que consiste en seleccionar un conjunto de artículos para cargar en un morral o mochila con capacidad máxima $$ W $$, de tal manera que se maximice el valor total de los artículos incluidos sin exceder la capacidad del morral. Los artículos no pueden partirse, y solo se puede tomar un número entero no negativo de cada tipo de artículo.

**Modelo Matemático:**

1. **Versión General (Artículos Repetibles):**

   - **Variables de decisión:** $$ x_j $$ (número de artículos del tipo $$ j $$):
     $$
     x_j \geq 0 \quad \text{(entero)} \quad \text{para } j = 1, 2, \dots, n
     $$

   - **Función objetivo:** Maximizar el valor total de los artículos:
     $$
     \text{Maximizar } z = \sum_{j=1}^n V_j x_j
     $$

   - **Restricción de capacidad:** Asegurar que el peso total no exceda la capacidad máxima $$ W $$:
     $$
     \sum_{j=1}^n P_j x_j \leq W
     $$

3. **Versión Binaria (Unidad por Artículo):**

   - **Variables de decisión:** $$ x_j $$ (variables binarias):
     $$
     x_j = 
     \begin{cases} 
     1, & \text{si el artículo } j \text{ es seleccionado}, \\
     0, & \text{en cualquier otro caso}.
     \end{cases}
     $$

   - **Función objetivo:** Maximizar el valor total de los artículos:
     $$
     \text{Maximizar } z = \sum_{j=1}^n V_j x_j
     $$

   - **Restricción de capacidad:** Asegurar que el peso total no exceda la capacidad máxima $$ W $$:
     $$
     \sum_{j=1}^n P_j x_j \leq W
     $$

   - **Restricción de enteros:** Todas las variables son binarias:
     $$
     x_j = 0 \text{ o } 1, \quad j = 1, 2, \dots, n
     $$

**Características:**

- El problema del morral es un modelo puro de programación entera, ya que todas sus variables toman valores enteros.
- La versión binaria es especialmente útil cuando solo se permite tomar un artículo de cada tipo.

#### Problema del Agente Viajero (Traveling Salesman Problem - TSP)

El problema del agente viajero es un problema clásico de optimización combinatoria que consiste en encontrar la ruta más corta que permita visitar un conjunto de ciudades exactamente una vez y regresar al punto de partida. El objetivo es minimizar el tiempo o costo total del recorrido.

**Modelo Matemático:**

1. **Definición del Problema:**

   - Se consideran $$ n $$ ciudades.
   - La distancia entre dos ciudades cualesquiera es conocida.
   - El agente viajero debe comenzar en una ciudad determinada, visitar cada ciudad exactamente una vez y retornar al punto de partida.

3. **Representación del Problema:**

   - El problema se puede representar como un grafo dirigido donde los vértices representan las ciudades y los arcos representan las rutas entre ellas.
   - Cada arco tiene un costo asociado, que corresponde a la distancia entre las ciudades.

5. **Variables de Decisión:**

   - $ x_{ijk} $ (variables binarias):
     $$
     x_{ijk} = 
     \begin{cases} 
     1, & \text{si el arco dirigido k va desde la ciudad } i \text{ a la ciudad } j, \\
     0, & \text{en cualquier otro caso}.
     \end{cases}
     $$
     donde $$ i, j = 1, 2, \dots, n $$ y $$ k = 1, 2, \dots, n-1 $$.

7. **Restricciones:**

   1. **Asignación única de arcos:** Solo un arco dirigido puede ser asignado a una posición $$ k $$ dada:

      $$
      \sum_{i \neq j} \sum_{j} x_{ijk} = 1, \quad k = 1, 2, \dots, n
      $$

   3. **Salida única desde cada ciudad:** Solo una ciudad puede iniciar un arco dirigido a otra ciudad específica:

      $$
      \sum_{k} \sum_{j} x_{ijk} = 1, \quad i = 1, 2, \dots, n
      $$

   5. **Llegada única a cada ciudad:** Solo una ciudad puede ser alcanzada desde una ciudad específica:

      $$
      \sum_{i} \sum_{k} x_{ijk} = 1, \quad j = 1, 2, \dots, n
      $$

   7. **Conexión de arcos:** Asegurar que el viaje esté formado por arcos conexos:

      $$
      \sum_{r} x_{ijk} = \sum_{r} x_{jr(k+1)}, \quad \text{para todo } j, k = 1, 2, \dots, n, \quad i \neq j
      $$

9. **Función Objetivo:**

   - Minimizar el costo total del recorrido:
     $$
     \text{Minimizar } z = \sum_{i} \sum_{j} \sum_{k} d_{ij} x_{ijk}, \quad i \neq j
     $$
     donde $ d_{ij} $ es la distancia más corta de la ciudad $$ i $$ a la ciudad $$ j $$.

**Características:**

- El problema del agente viajero es NP-difícil, lo que significa que su resolución exacta es computacionalmente intensiva incluso para valores pequeños de $$ n $$.
- Para $$ n = 10 $$, el problema involucra 1000 variables y 130 restricciones.
- Se han desarrollado algoritmos especiales (como branch-and-bound, heurísticas y metaheurísticas) para resolver eficientemente instancias grandes del problema.

#### Problemas de Cubrimiento

Los problemas de cubrimiento son una clase de problemas de optimización que involucran la selección de un conjunto mínimo de elementos (como actividades, rutas o recursos) para satisfacer ciertas restricciones o cubrir ciertos requerimientos. Estos problemas surgen en diversas aplicaciones prácticas, como la entrega de órdenes, la asignación de vuelos a tripulaciones y la localización de facilidades.

**Ejemplo: Problema de Entrega de Órdenes**

Considérese el problema de entregar órdenes desde un almacén a $$ m $$ destinos diferentes, donde cada destino recibe su orden de entrega. Un repartidor puede combinar un máximo de $$ k $$ órdenes para ser entregadas simultáneamente. La determinación de una combinación factible depende de la ruta asignada a cada repartidor, ya que un mismo destinatario podría caer en más de una ruta, lo que requiere decidir qué repartidor realiza la entrega.

1. **Definiciones:**

   - **Actividad:** Una "actividad" se define como una combinación factible de una, dos, ..., o $$ k $$ órdenes.
   - **Costo de la actividad:** Sea $$ C_j $$ el costo asociado con la actividad $$ j $$.
   - **Variable binaria:** $$ X_j $$ (variable binaria):

     $$
     X_j = 
     \begin{cases} 
     1, & \text{si la actividad } j \text{ se lleva a cabo}, \\
     0, & \text{en cualquier otro caso}.
     \end{cases}
     $$

3. **Modelo Matemático:**

   - **Función objetivo:** Minimizar el costo total de todas las actividades:

     $$
     \text{Minimizar } z = \sum_{j=1}^n C_j X_j
     $$

   - **Restricciones de cubrimiento:** Asegurar que todas las órdenes sean entregadas:

     $$
     \sum_{j=1}^n \alpha_j X_j = \alpha_0
     $$

     donde:
     - $$ \alpha_j $$ es el vector columna de $$ m $$ componentes, con la componente de lugar $$ i $$ igual a 1 si la actividad $$ j $$ realiza la entrega $$ i $$, y cero en cualquier otro caso.
     - $$ \alpha_0 $$ es el vector columna con $$ m $$ componentes iguales a 1, representando que todas las $$ m $$ órdenes deben ser cubiertas.

**Generalización a Otros Problemas Prácticos:**

- **Asignación de Vuelos a Tripulaciones:** En este problema, cada etapa de vuelo corresponde a una restricción, y se requiere que una de las tripulaciones disponibles sea asignada a dicha etapa de vuelo. Cada actividad (representada por un vector columna) corresponde a un período posible de asignación para una tripulación específica. El objetivo es minimizar el costo total de todas las asignaciones.
  
- **Localización de Facilidades:** Un modelo similar puede ser construido para el problema de localización de facilidades, donde se busca seleccionar un conjunto mínimo de ubicaciones para satisfacer ciertas demandas.

**Analogía con la Teoría de Grafos:**

El nombre "Problema de Cubrimiento" surge por analogía con un caso similar en la teoría de grafos. Dado un grafo $$ G = (V, A) $$ con $$ n $$ vértices y $$ p $$ aristas, se requiere encontrar un "cubrimiento" con el mínimo número de aristas, donde un "cubrimiento" se define como un subconjunto de aristas tal que cada uno de los $$ n $$ vértices es incidente a alguna arista del subconjunto.

1. **Matriz de Incidencia:**

   - La matriz de incidencia $$ I = (\alpha_1, \alpha_2, \dots, \alpha_n) $$ representa el grafo, donde cada columna corresponde a una arista y cada fila a un vértice.
   - Cada columna $$ \alpha_j = (\alpha_{1j}, \alpha_{2j}, \dots, \alpha_{nj})^T $$ tiene exactamente dos componentes iguales a 1 (correspondientes a los vértices conectados por la arista $$ j $$), y el resto de las componentes son cero.

3. **Variables Binarias:**

   - $$ X_j = 1 $$ si la arista $$ j $$ está incluida en el cubrimiento, y $$ X_j = 0 $$ en cualquier otro caso.

5. **Función Objetivo:**

   - Minimizar el número total de aristas en el cubrimiento:
     $$
     \text{Minimizar } z = X_1 + X_2 + \cdots + X_n
     $$

**Características:**
- Los problemas de cubrimiento son modelos de programación entera mixta, ya que involucran variables binarias para decidir qué actividades o elementos se seleccionan.
- Estos problemas son NP-difíciles en general, pero existen técnicas específicas (como branch-and-bound, heurísticas y métodos de relajación) para resolver instancias grandes de manera eficiente.

## Ejercicios de TSP/TP de Semestres Pasados

### 2024-2
{% include toggle.html id="359__obj_01__mr_2024_2" content=" " %}
### 2024-1
{% include toggle.html id="359__obj_01__mr_2024_1" content=" " %}
### 2023-2
{% include toggle.html id="359__obj_01__mr_2023_2" content=" " %}
### 2023-1
{% include toggle.html id="359__obj_01__mr_2023_1" content=" " %}
### 2022-2
{% include toggle.html id="359__obj_01__mr_2022_2" content=" " %}
### 2022-1
{% include toggle.html id="359__obj_01__mr_2022_1" content=" " %}
### 2021-2
{% include toggle.html id="359__obj_01__mr_2021_2" content=" " %}
### 2021-1
{% include toggle.html id="359__obj_01__mr_2021_2" content=" " %}
